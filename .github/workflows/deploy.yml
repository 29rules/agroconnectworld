name: Deploy (Skeleton)

on:
  workflow_dispatch:
    inputs:
      env:
        type: choice
        description: Environment
        required: true
        options: [staging, production]
        default: staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deploy placeholder"
          echo "Target env: ${{ github.event.inputs.env }}"
          echo "Next: pull images from GHCR on your server/orchestrator and restart services."
